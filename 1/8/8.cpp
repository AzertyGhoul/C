#include<iostream>

using namespace std;

int main(){
    double a,b,sum,difference,multiply;
    cout << "Enter numbers ";
    cin >> a >> b;
    sum = a + b;
    difference = a - b;
    multiply = a * b;
    cout << "Sum = " << sum << "\ndifference = " << difference << "\nMultiply = " << multiply ;
}